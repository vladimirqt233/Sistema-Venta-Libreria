server:
  port: 8090

spring:
  application:
    name: ms-gateway-service
  profiles:
    active: development
  config:
    import: optional:configserver:http://${CONFIG_SERVER_USER:root}:${CONFIG_SERVER_PASSWORD:123456}@localhost:7071
