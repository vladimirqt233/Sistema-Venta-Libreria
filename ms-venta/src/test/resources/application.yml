spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ms_venta?useSSL=false&serverTimezone=UTC
    username: admin123
    password: admin123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQLDialect
  cloud:
    config:
      enabled: false