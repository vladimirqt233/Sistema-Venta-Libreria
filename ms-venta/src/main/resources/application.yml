server:
  port: 8082

spring:
  application:
    name: ms-venta-service

  profiles:
    active: development

  config:
    import: "optional:configserver:http://${CONFIG_SERVER_USER}:${CONFIG_SERVER_PASSWORD}@localhost:7070"

  datasource:
    url: jdbc:mysql://localhost:3306/ms_venta
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true